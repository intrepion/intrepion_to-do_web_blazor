@page "/admin/UrlNamePlaceholder-edit"
@page "/admin/UrlNamePlaceholder-edit/{id}"
@inject IEntityNamePlaceholderAdminService EntityNamePlaceholderAdminService
@inject AuthenticationStateProvider GetAuthenticationStateAsync
@inject NavigationManager NavigationManager
@rendermode InteractiveAuto

<section>
    @if (Id == null)
    {
        <h1>HumanNamePlaceholder Role</h1>
    }
    else
    {
        <h1>Edit HumanNamePlaceholder</h1>
    }
    <article>
        <EditForm class="container" Model="LowercaseNamePlaceholder" OnSubmit="HandleSubmit">
            @* <div class="row">
                <div class="col">
                    <label>
                        PropertyNamePlaceholder:
                        <InputText @bind-Value="LowercaseNamePlaceholder.PropertyNamePlaceholder">@LowercaseNamePlaceholder.PropertyNamePlaceholder</InputText>
                    </label>
                </div>
            </div> *@
            <div class="row">
                <div class="col">
                    @if (Id != null)
                    {
                        <button @onclick="Delete" type="button" class="btn btn-danger">Delete</button>
                    }
                </div>
                <div class="col">
                    <a href="/admin/UrlNamePlaceholder-list">Cancel</a>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </div>
        </EditForm>
    </article>
</section>

@code {
    [Parameter]
    public string? Id { get; set; }
    EntityNamePlaceholder LowercaseNamePlaceholder { get; set; } = new EntityNamePlaceholder();

    protected override async Task OnParametersSetAsync()
    {
        if (Id == null)
        {
            return;
        }

        var result = await EntityNamePlaceholderAdminService.GetByIdAsync(Id);

        if (result == null)
        {
            LowercaseNamePlaceholder = new EntityNamePlaceholder();
        }
        else
        {
            LowercaseNamePlaceholder = result;
        }
    }

    async Task HandleSubmit()
    {
        var authState = await GetAuthenticationStateAsync.GetAuthenticationStateAsync();
        var userName = authState.User.Identity?.Name;

        if (userName == null)
        {
            return;
        }

        if (userName == null)
        {
            return;
        }

        if (LowercaseNamePlaceholder == null)
        {
            return;
        }

        if (Id == null)
        {
            var dbEntityNamePlaceholder = await EntityNamePlaceholderAdminService.AddAsync(userName, LowercaseNamePlaceholder);

            if (dbEntityNamePlaceholder == null)
            {
                return;
            }

            NavigationManager.NavigateTo($"/admin/UrlNamePlaceholder-edit/{dbEntityNamePlaceholder.Id}");
        }
        else
        {
            await EntityNamePlaceholderAdminService.EditAsync(userName, (string)Id, LowercaseNamePlaceholder);
        }
    }

    async Task Delete()
    {
        if (Id == null)
        {
            return;
        }

        var authState = await GetAuthenticationStateAsync.GetAuthenticationStateAsync();
        var userName = authState.User.Identity?.Name!;

        if (userName == null)
        {
            return;
        }

        await EntityNamePlaceholderAdminService.DeleteAsync(userName, Id);
        NavigationManager.NavigateTo($"/admin/UrlNamePlaceholder-list");
    }
}
