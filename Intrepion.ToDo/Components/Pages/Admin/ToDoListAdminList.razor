@page "/admin/UrlNamePlaceholder-list"
@rendermode InteractiveServer
@inject IToDoListAdminService ToDoListAdminService
@attribute [StreamRendering]

<section>
    <h1>HumanNamePlaceholder List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    @* ListHeaderCodePlaceholder *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (TableLowercaseNamePlaceholder == null)
                {
                    <tr>
                        @* LoadingCountCodePlaceholder *@
                    </tr>
                }
                else if (TableLowercaseNamePlaceholder.Count == 0)
                {
                    <tr>
                        @* ZeroCountCodePlaceholder *@
                    </tr>
                }
                else
                {
                    @foreach (var toDoList in TableLowercaseNamePlaceholder)
                    {
                        <tr>
                            <td>@toDoList.Id</td>
                            @* ListItemCodePlaceholder *@
                            <td>
                                <NavLink Href="@("/admin/UrlNamePlaceholder-edit/" + toDoList.Id)">Edit</NavLink>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <NavLink Href="/admin/UrlNamePlaceholder-edit">Create</NavLink>
    </article>
</section>

@code {
    List<ToDoListAdminDataTransferObject>? TableLowercaseNamePlaceholder = null;

    protected override async Task OnInitializedAsync()
    {
        TableLowercaseNamePlaceholder = await ToDoListAdminService.GetAllAsync();
    }
}
